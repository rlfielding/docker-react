version: '3'
services:
  frontend:
    build:
      context: .
      dockerfile: dockerfile.dev
    ports:
      - "3000:3000"
    volumes:
      - /app/node_modules
      - .:/app
  tests:
    build:
      context: .
      dockerfile: dockerfile.dev
    command: ["npm", "run", "test"]
    volumes:
      - /app/node_modules
      - .:/app